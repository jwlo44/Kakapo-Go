using UnityEngine;
using System.Collections;

public class shootCat : MonoBehaviour {
/*	public static float delay = 0;
	bool shooting = false;
	int countdown = 0;
	int countdownMax = 60;
	public GameObject cat;
	
	// Use this for initialization
	void Start () {
		StartCoroutine (WaitBeforeShooting (delay));
		delay += .2;
	
	}
	
	// Update is called once per frame
	void Update () {
		if (shooting && countdown <= 0) {
			countdown = countdownMax;
			shootOneCat ();
		} else {
			countdown--;
		}
	
	}

	IEnumerator WaitBeforeShooting (float delay) {
		yield return new WaitForSeconds(delay);
		shooting = true;
	}

	private void shootOneCat() {
		GameObject c = GameObject.Instantiate (cat, this.transform.position, Quaternion.identity);
	}*/
}
